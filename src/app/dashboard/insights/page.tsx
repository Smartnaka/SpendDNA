import { Card } from "@/components/ui/card";import { generateInsights } from "@/lib/analytics/engine";import { transactions } from "@/lib/sample-data";export default function Insights(){return <div><h1 className="text-4xl font-black">AI Insights</h1><div className="mt-8 grid gap-4 md:grid-cols-2">{generateInsights(transactions).map(i=><Card key={i.title}><p className="text-sm uppercase text-teal-300">{i.tone}</p><h2 className="mt-2 text-2xl font-bold">{i.title}</h2><p className="mt-3 text-slate-300">{i.body}</p></Card>)}</div></div>}
