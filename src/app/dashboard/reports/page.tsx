import { Card } from "@/components/ui/card";import { analyzeTransactions } from "@/lib/analytics/engine";import { transactions } from "@/lib/sample-data";export default function Reports(){const a=analyzeTransactions(transactions);return <div><h1 className="text-4xl font-black">Financial Report</h1><Card className="mt-8"><p className="text-teal-300">Financial Score</p><h2 className="text-7xl font-black">86</h2><p className="mt-4 text-slate-300">Strong positive cash flow, diversified income, and controlled recurring spending.</p></Card><div className="mt-6 grid gap-4 md:grid-cols-3">{["Income Summary","Expense Summary","Share Card"].map(x=><Card key={x}><h2 className="text-xl font-bold">{x}</h2><p className="mt-3 text-slate-400">Income ₦{a.totalIncome.toLocaleString()} · Expenses ₦{a.totalExpenses.toLocaleString()} · Net ₦{a.netCashFlow.toLocaleString()}</p></Card>)}</div></div>}
