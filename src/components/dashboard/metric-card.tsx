import { Card } from "@/components/ui/card";import { currency } from "@/lib/utils";export function MetricCard({label,value,trend}:{label:string;value:number;trend?:number}){return <Card className="p-5"><p className="text-sm text-slate-400">{label}</p><p className="mt-3 text-2xl font-bold tracking-tight">{label.includes("Rate")||label.includes("Growth")?`${value.toFixed(1)}%`:currency.format(value)}</p>{trend!==undefined&&<p className={trend>=0?"text-teal-300":"text-rose-300"}>{trend.toFixed(1)}% vs previous</p>}</Card>}
