import { cn } from "@/lib/utils";import Link from "next/link";type Props=React.ButtonHTMLAttributes<HTMLButtonElement>&{href?:string;variant?:"primary"|"secondary"|"ghost"};export function Button({className,variant="primary",href,...props}:Props){const cls=cn("focus-ring inline-flex h-11 items-center justify-center rounded-full px-5 text-sm font-semibold transition",variant==="primary"&&"bg-teal-400 text-slate-950 hover:bg-teal-300",variant==="secondary"&&"border border-white/15 bg-white/10 text-white hover:bg-white/15",variant==="ghost"&&"text-slate-300 hover:text-white",className);return href?<Link className={cls} href={href}>{props.children}</Link>:<button className={cls}{...props}/>}
